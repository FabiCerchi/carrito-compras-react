import React from 'react';

const Header = ({titulo}) => {
    return (
        <>
            <h2>{titulo}</h2>
            <h4>Soy el header (Aca poner un menu)</h4>
        </>
      );
}
 
export default Header;